## Quick-tier depth

Report at most 2 design issues, and only ones that are locally obvious from
the diff itself — don't search the wider codebase for comparison patterns at
this tier. If the diff is a small, self-contained change that fits its
surroundings, an empty findings list is the expected result.
