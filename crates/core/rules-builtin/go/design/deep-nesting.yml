# `language: Go` here only picks which fixture extension/filename
# rule_pack_conformance-style testing uses — complexity.rs's deep-nesting
# check runs uniformly across Go/Java/Kotlin from this one threshold
# value, it isn't gated by this field at runtime.
id: deep-nesting
language: Go
category: design
severity: warning
kind: threshold
metric: deep-nesting
threshold: 4
message: "Deep nesting"
