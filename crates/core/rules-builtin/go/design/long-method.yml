# `language: Go` here only picks which fixture extension/filename
# rule_pack_conformance-style testing uses — complexity.rs's long-method
# check runs uniformly across Go/Java/Kotlin from this one threshold
# value, it isn't gated by this field at runtime.
id: long-method
language: Go
category: design
severity: warning
kind: threshold
metric: long-method
threshold: 80
message: "Long method"
