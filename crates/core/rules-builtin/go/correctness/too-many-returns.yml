# `language: Go` here only picks which fixture extension/filename
# rule_pack_conformance-style testing uses — complexity.rs's too-many-
# returns check runs uniformly across Go/Java/Kotlin from this one
# threshold value, it isn't gated by this field at runtime.
id: too-many-returns
language: Go
category: correctness
severity: warning
kind: threshold
metric: too-many-returns
threshold: 4
message: "Too many return points"
